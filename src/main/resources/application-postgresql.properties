#################################################
# === PostgreSQL Connection ===
#################################################

spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/${POSTGRESQL_DB_NAME}?currentSchema=${POSTGRESQL_DB_SCHEMA}
spring.datasource.username=${POSTGRESQL_DB_USERNAME}
spring.datasource.password=${POSTGRESQL_DB_PASSWORD}